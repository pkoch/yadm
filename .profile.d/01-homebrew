prepend_path_if_missing "/opt/homebrew/bin"
prepend_path_if_missing "/home/linuxbrew/.linuxbrew/bin"

if command -v brew &>/dev/null; then
    export HOMEBREW_NO_ENV_HINTS=1
    . <(brew shellenv)
fi
