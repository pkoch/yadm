#!/usr/bin/env python3
import sys, base64

if len(sys.argv) != 2:
    print(f"Usage: {sys.argv[0]} <hexstring>")
    sys.exit(1)

hex_str = sys.argv[1]
try:
    data = bytes.fromhex(hex_str)
except ValueError:
    print("Invalid hex input.")
    sys.exit(1)

print(base64.b64encode(data).decode())
