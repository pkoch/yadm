echo Initial state
yadm status -sb

gpg --import ~/.gnupg/pubring.asc
gpg --export -a > ~/.gnupg/pubring.asc

rustup self update
rustup update

asdf plugin update --all
asdf update

bin/yadm-update

echo Final state
yadm status -sb
