function maybe_regen_and_source_completion() {
    BIN_NAME="$1"

    if [ ! -f "~/.bashrc.d/.$BIN_NAME.cached" ]
    then
        "~/.config/yadm/update.d/completion-$BIN_NAME"
    fi

    . "~/.bashrc.d/.$BIN_NAME.cached"
}
