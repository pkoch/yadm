function maybe_regen_and_source_completion() {
    BIN_NAME="$1"

    if [ ! -f "$HOME/.bashrc.d/.$BIN_NAME.cached" ]
    then
        "$HOME/.config/yadm/update.d/completion-$BIN_NAME"
    fi

    . "$HOME/.bashrc.d/.$BIN_NAME.cached"
}
